{
    "name": "garf-api",
    "module": "src/index.ts",
    "type": "module",
    "scripts": {
        "build": "bun build . --compile --outfile=build/garf-api"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "@types/node": "^20.11.29"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    }
}
